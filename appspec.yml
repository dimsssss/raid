version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/app
    overwrite: yes
    runas: ec-user

permissions:
  - object: /
    pattern: '**'
    owner: ec2-user

hooks:
  ApplicationStart:
    - location: start_server.sh
